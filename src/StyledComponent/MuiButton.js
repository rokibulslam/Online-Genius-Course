import { Button } from "@mui/material";
import { styled } from "@mui/styles";

const MuiButton = styled(Button)({
  background: "linear-gradient(60deg,  #00a8d5, #00dad8)",
  color: "#fff",
  padding: "13px 30px",
});

export default MuiButton;
